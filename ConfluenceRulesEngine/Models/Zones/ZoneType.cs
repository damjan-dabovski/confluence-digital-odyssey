namespace ConfluenceRulesEngine.Models.Zones
{
    public enum ZoneType
    {
        Hand,
        Deck,
        Trash
        // TODO determine if the individual slots each have a separate type value,
        // or just a catch-all 'Slot' value
    }
}
